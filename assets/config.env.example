# Copy to config.env and fill in your keys (do not commit config.env)
GEMINI_API_KEY=
# Web client from booqlyapp-83777 — Firebase Auth → Google → Web client ID
# Must start with 87414724762- (NOT 982263059367- from another GCP project)
GOOGLE_WEB_CLIENT_ID=
# After editing, run: .\scripts\sync-google-oauth.ps1

# Email is sent server-side by the sendAuthEmail Cloud Function (Gmail SMTP).
# Put GMAIL_USER / GMAIL_APP_PASSWORD / EMAIL_FROM in functions/.env,
# NOT here. See firebase/EMAIL_SETUP.md.
